# Handoff — 2026-06-27

Estado: `main` limpio, todo pusheado. Último commit `51e48ce`.

## Lo que se terminó esta sesión

### 1. Páginas legales (Vercel/landing)
- `landing/src/pages/terminos.astro` — Términos y Condiciones (23 secciones, marco legal AR: Ley 24.240, 25.326, 27.350, 23.737, CCyC).
- `landing/src/pages/privacidad.astro` — Privacidad y Seguridad (14 secciones, Ley 25.326 / AAIP).
- `landing/src/layouts/LegalLayout.astro` — shell de documento legal (fondo claro, "última actualización").
- `landing/src/components/Footer.astro` — columna "Legales" linkea ambas (`/terminos`, `/privacidad`).
- Versión vigente de los documentos: **2026-06-27**.

### 2. Aceptación de términos en el registro
- `client/src/pages/Register.jsx` — checkbox obligatorio `.rg-terms` (links a `/terminos` y `/privacidad` en `_blank`), botón deshabilitado sin aceptar. El body manda `terms_accepted: true` + `terms_version: "2026-06-27"`.
- `server/index.js` — registro de aceptación **server-side**:
  - Migración auto: `users.terms_accepted_at` (timestamptz), `terms_version` (text), `terms_accepted_ip` (text).
  - `POST /auth/register` exige `terms_accepted===true` (400 si falta) y persiste `NOW()`, versión e IP (`x-forwarded-for`/`req.ip`).
  - Constante `TERMS_VERSION = "2026-06-27"` cerca de `const app = express()`.

## PENDIENTE para mañana

1. **Verificar deploy + evidencia legal** (lo que quedó sin probar):
   - Render tiene que redeployar el server para que (a) se cree la columna y (b) el endpoint exija/guarde la aceptación. La columna se crea sola en el primer boot con el código nuevo.
   - Una vez deployado: hacer un registro de prueba y confirmar que `terms_accepted_at` / `terms_version` / `terms_accepted_ip` quedan grabados en `users`.
   - Vercel ya sirve el client + las páginas legales (no depende de Render).

## ✅ VERIFICADO — 2026-07-01

Deploy confirmado en producción, los tres frentes probados en vivo:

- **Server (Render, `holistic-portal-api.onrender.com`)** — código nuevo live:
  - Probe `POST /auth/register` **sin** `terms_accepted` → `400 "Tenés que aceptar los Términos..."` (línea 960). El check de términos corre antes de validar el invite.
  - Probe **con** `terms_accepted:true` + invite falso → pasa términos, frena en `"Código de invitación inválido"`. Flujo normal intacto.
  - Ninguna sonda creó usuario (invite inválido). Como el server booteó con el código nuevo, las columnas `terms_accepted_at` / `terms_version` / `terms_accepted_ip` ya se auto-crearon (`ADD COLUMN IF NOT EXISTS`, líneas 306-308).
- **Landing legal (Vercel, `portal-starter.vercel.app`)** — `/terminos` y `/privacidad` → 200, título correcto, "Última actualización: 27 de junio de 2026".
- **Portal client (Vercel)** — chunk `Register-_5-s7P7R.js` contiene `rg-terms`, `terms_accepted` y versión `2026-06-27`.

**Único cabo suelto (requiere invite real):** confirmar contra la DB de Render que un registro *exitoso* graba `terms_accepted_at`/`version`/`ip` en la fila de `users`. El código path está verificado (INSERT líneas 982-988); falta la evidencia con un usuario real. No se probó para no crear un usuario basura en prod.
> Nota: el dominio custom `hgrowshop.com/portal` es el WordPress del cliente (404). El deploy real del portal-starter vive en `portal-starter.vercel.app`. `api.hgrowshop.com` todavía no resuelve DNS; usar la URL `onrender.com`.

## Recordatorios de entorno (no perder tiempo mañana)
- **DB real + Cloudinary (`drdha21z8`) viven SOLO en Render.** El `.env` local apunta a una DB STALE (Cloudinary dummy `my-cloud`). No probar migraciones contra la DB local esperando datos reales.
- **Detección de deploy en Vercel:** los hashes del bundle SIEMPRE difieren (env-baked `VITE_API_URL`). Detectar deploy por *cambio* de hash del bundle live o grepeando strings únicos en `/portal/assets/AdminPanel-*.js`.
- `server/index.js` es "binary" para grep (tiene emojis) → usar `grep -a`.
- Checks de build: `node -c server/index.js` y `cd client && npm run build`.

## Hilos previos ya cerrados (contexto, no acción)
- Galería de kits reactiva por medida + "medida destacada" elegible por admin.
- Editor de imágenes por medida dentro del producto (familias y kits).
- Hero del kit destacado por medida, subible por el admin.
- Migración de renders de carpeta → Cloudinary (endpoint admin + botón).
- Shop: "FERTILIZANTES PREMIUM" → "fertilizantes de precisión".
- Sistema de puntos: sección promo en landing (`HolisticPoints.astro`) + tab "Cómo funciona" en `/coins`.
