# Handoff — 2026-07-02/03

Estado: `main` limpio, **todo commiteado y pusheado** a `origin/main`. Último commit `f217a59`.
Sesión larga (~30 commits, `9d945e8..f217a59`). Todos los cambios ya están en producción
(Vercel para landing+client, Render para el server — **las migraciones de shop corren al boot
del server**, así que se aplican cuando Render redeploye).

---

## Lo que se hizo esta sesión

### HOME (landing)
- **Videos del hero nuevos + optimizados**: PC = `BANNER-HOLISTIC-INICIO.mp4` (1376×768, 0.93MB),
  mobile = `BANNER-HOLISTIC-03.mp4` (1080×1920 vertical, 1.80MB). Ambos x264 CRF 24/25, faststart,
  **sin audio**, SSIM ~0.9955. Posters actualizados al frame final (el hero congela ahí).
  Fuente: `IMAGENES-HOLISTIC/ULTIMAS-IMAGENES/BANNERS-SLIDER/`.
- **`object-fit`**: se probó `contain` (para no perder potes) pero descuadraba el PC → **se volvió a
  `cover`** full-bleed. Con `cover` en pantallas más angostas que 16:9 se recorta algo de los tarros
  de los costados (trade-off aceptado por el cliente con el video nuevo).
- **Empalme HumanSupport → OrganicConnector** (mucho ir y venir, ver PENDIENTE/notas): terminó como
  **un único degradé continuo verde oscuro → verde → mint → blanco**, sin línea. Claves del fix final:
  (1) ambos gradientes con la MISMA pendiente en la unión (OC arranca suave, sin acelerar),
  (2) overlays de HS (glow radial + grain) enmascarados antes del borde,
  (3) solape `.oc { margin-top: -8px }` para matar el gap sub-pixel.
  **Verificado renderizando la página real con Playwright headless** (ver notas de entorno).
- **Cursor nativo tipo Apple**: se removió `<CustomCursor>` de `BaseLayout` (dot+ring laggy). Ahora
  cursor del sistema. `CustomCursor.astro` / `cursor-animations.js` quedan huérfanos (por si se reactiva).
- **Mensaje de envío**: "Envío en 48hs" → **"Despachamos en 24hs"** en todas las superficies.

### INTERNA RACE
- **"FÓRMULA ADAPTABLE"**: reemplaza el bigStat "5 fertilizantes" con statement editorial + reveal L→R.
- **Copy intro**: "Para todo el ciclo indoor y outdoor…" (sin "Cinco fórmulas").
- **Violeta Race 3 → gris** (`#8E24AA`/`#4A148C` → `#555`/`#333`).
- **RACE · BACKSTAGE** (los highlights): los potes 1L usan el **tarro dosificador de costado**
  (`-tarro-1l.webp`) en vez del render f1.

### INTERNA ELITE
- **Bidones 5/10/20L dobles**: formats + highlights usan las imágenes "juntos" (Parte 1 + Parte 2).
  Highlight 04 usa `POTE-DOBLE-5LT.png` (no hay PNG doble 20L transparente).
- **Guías**: 1ª = **Hidroponía** (elite-1.pdf), 2ª = **Sustrato** (elite-2.pdf).

### INTERNA PRO
- **PDFs de guías optimizados para mobile**: tenían páginas descomunales (MediaBox ~9866×10061) y
  25–55MB → salían "expandidos". Re-render Ghostscript a **1600×1600, /ebook**: 4.0/4.5/9.2MB.
  Comando: `gs -sDEVICE=pdfwrite -dPDFSETTINGS=/ebook -dFIXEDMEDIA -dDEVICEWIDTHPOINTS=1600 -dDEVICEHEIGHTPOINTS=1600 -dPDFFitPage ...`

### SHOP
- **Texto catálogo en Gotham** (`.cat-sub`, era Fraunces italic).
- **Acento del shop violeta rojizo → gris**: en `client/index.html` bloque `.theme-light`,
  `--c-accent: #5B3644 → #555555`, `--c-accent-2 → #333`. Afecta TODO el shop (una sola variable).
  **NO se tocó** el mint `#A7F5C8` del tema oscuro (portal).
- **Packs de puntos → moneda HOLISTIC oficial**: bug estaba en `ShopCheckout.jsx` (renderizaba
  `primary_image` crudo sin `fixImageUrl`). Ahora todas las superficies (catálogo/ficha/carrito/checkout)
  mapean el SVG diamante → `moneda-holistic.webp` vía `fixImageUrl` (mapeo ya existía en `shopImages.js`).
- **Sacar 1L de Day-0 y Bio** (sin stock): migración desactiva `bio-estimulante-1l`/`day-0-1l` + limpia
  `meta.presentaciones` a `["500ml"]`.
- **Elite Parte 1 completada**: 250ml ($22.304), 5L ($337.749), 10L ($568.372) — precios REALES del
  shop (hgrowshop), no los placeholders. 5L/10L usan la **foto doble** del carrito. Se desactivaron
  `elite-max-5l`/`10l` (duplicaban). SKUs nuevos: `elite-parte-1-250ml/5l/10l`.
- **Cross-sell "Sumá para acompañar"** (¡era server-side!): `CROSS_SELL_BY_LINE` en
  `server/routes/shop.js` → **race = Race 3 PK 1ª/2ª · pro = Race 4 + Race 2**. Antes mandaba
  bio/day0/cloner. El server self-filtra el producto actual. También hay default client-side espejo.

### ADMIN (editar producto)
- **Editores pre-cargados desde la interna**: al abrir "Editar", los campos (beneficios, características,
  datos técnicos, cross-sell) arrancan con el contenido actual de la ficha (`lineDetails` + defaults por
  línea) cuando `meta.editorial` está vacío. Al guardar se persiste en `meta.editorial`.
- **Nueva sección editable "También de esta línea"** (related): ProductPicker → `editorial.related_slugs`.
  ShopProduct usa esos slugs si existen; vacío = automático por categoría.
- **Panel admin responsive**: `data-staff-page` en el root activa `staff-mobile.css` (tablas scroll,
  grids 1col, inputs capped) + tabs scrollables + modales con max-height/scroll.
- **Sync contenido shop desde internas**: `scripts/gen-line-details.mjs` regenera
  `client/src/data/lineDetails.js` desde `landing/src/data/productDetails.js`. Correr con
  `node scripts/gen-line-details.mjs` cuando cambien las internas.

### COINS
- **Puntos IG alineados al doc v9 §9.2**: like 2, comentario 5, story 7, envase 17, planta 33,
  etapa 50, ciclo 167 (`server/routes/coins.js` `IG_ACTIONS`). Server-authoritative.
- **Moneda más grande**: `COIN_SCALE = 1.4` global en `Coins.jsx` + balance/referidos más prominentes.
- **Moneda del balance** bajada `translateY(8px)` para alinear con el número.

---

## PENDIENTE / a confirmar con el cliente

1. **Empalme HS→OC**: quedó liso en el render de Playwright, pero el cliente veía la línea (era CSS
   **cacheado** — su screenshot era del estado viejo). **Confirmar con hard refresh** que ahora se ve
   liso. Si todavía se corta, el render de referencia está en el scratchpad de la sesión.
2. **Banner PC con `cover`**: puede recortar tarros de los costados en pantallas angostas. Si molesta,
   la alternativa es un banner compuesto más al centro (o volver a `contain`, que no le gustó).
3. **Highlight 04 interna Elite**: usa el doble 5L (no hay render doble 20L transparente). Si el cliente
   pasa un PNG doble 20L sin fondo, cambiarlo.
4. **Contenido editorial en DB**: hoy el shop muestra `lineDetails` (default) y el admin pre-carga eso;
   sólo se persiste a `meta.editorial` cuando el admin guarda cada producto. Si se quiere dejarlo escrito
   en la DB de TODOS los productos sin abrir uno por uno, falta una migración que popule `meta.editorial`
   desde las internas (ofrecido, no hecho).

## Notas de entorno (útiles)

- **Render de la landing para verificar visual** (clave para el empalme): hay un Chromium de Playwright
  cacheado en `~/.cache/ms-playwright/chromium-1223/`. Receta:
  `cd landing && npm i playwright-core --no-save`, `npm run build`, servir `dist/` con
  `python3 -m http.server 4321`, y un script node con `chromium.launch({executablePath: ...})` que
  scrollea al `[data-organic-connector]` y hace `screenshot`. (El script quedó en el scratchpad como
  referencia; se puede recrear.)
- Sólo `ffmpeg` y `gs` (Ghostscript) disponibles para media (no cwebp/imagemagick para conversión, pero
  `identify` de imagemagick sí anda para leer dims).
- Los videos fuente del cliente están en `IMAGENES-HOLISTIC/ULTIMAS-IMAGENES/BANNERS-SLIDER/`.
