# HANDOFF — 2026-07-16 — Sin "1 punto = $4.000" + dije a 1.500 pts en landing

> Continúa `HANDOFF-2026-07-14-b.md`. Todo commiteado + pusheado a
> `origin/main`. Commit de la sesión: `b9844af` (copy de puntos en portal +
> landing). Builds de client y landing verificados antes del push.

## 1. 🧹 Se quitó la equivalencia "1 punto = $4.000" de todos los textos

Pedido del cliente: que no aparezca más "1 punto = $4.000" en ningún lado.
Eligió explícitamente (pregunta directa) **sacar la equivalencia en $** en vez
de poner un valor nuevo, y **NO tocar la config real** (`peso_per_point` en
DB queda en 4000).

Cambios (todo cosmético, cero lógica):

- `client/src/pages/Coins.jsx` — panel **TUS PUNTOS**: se eliminó la línea
  "≈ $X en valor de canje · 1 punto = $4.000".
- `client/src/pages/Coins.jsx` — tab **Cómo funciona**, sección Reglas: se
  eliminó la regla "Cada punto vale $4.000 de valor de canje.".
- `landing/src/components/HolisticPoints.astro` — texto fino: antes
  "1 punto = $4.000 de valor de canje · acumulás 1 punto cada $12.000 · los
  puntos no vencen", ahora sólo "Acumulás 1 punto cada $12.000 · los puntos
  no vencen".

### 🪙 Follow-up misma sesión: también se quitó "1 moneda = $4.000"

El cliente después pidió sacar también la equivalencia de las monedas.
Cambios (siempre sólo copy — la config `peso_per_point` y el circuito de
pago quedan intactos):

- `Coins.jsx` panel **TUS MONEDAS**: se eliminó "≈ $X para pagar pedidos ·
  1 moneda = $4.000".
- `Coins.jsx` **Cómo funciona** → monedas: se eliminó "Cada moneda vale
  $4.000." de "Pagan tus pedidos".
- Limpieza: `pesoPerPoint` (estado + prop de `ComoFunciona`) quedó sin uso
  en Coins.jsx y se removió. `ShopCheckout.jsx` lo sigue usando de verdad
  (calcula `costMonedas`) — no se tocó.
- El checkout nunca mostró "1 moneda = $X"; muestra "este pedido cuesta N
  monedas", que es funcional y queda.

OJO: si algún día piden CAMBIAR el valor (no ocultarlo), eso modifica cuánto
descuentan las monedas al pagar — server `routes/checkout.js` lee
`point_config.peso_per_point`.

### ⚠️ Lo que se dejó A PROPÓSITO

- "Ganás 1 punto cada $12.000" (tasa de acumulación, `earn_per_point`) sigue
  en portal y landing — el pedido era sólo la equivalencia de canje.
- `peso_per_point` sigue viajando en la API (`GET /coins/:userId` →
  `peso_per_point`, `value_ars`) — el front simplemente ya no lo muestra
  para puntos.

## 2. 🏷️ Dije Don Rouch: landing decía 500, vale 1.500

`landing/src/components/HolisticPoints.astro` mostraba "Dije Don Rouch ·
500 puntos · aspiracional" — valor viejo. En DB vale **1.500** desde el
2026-07-11 (migración en `server/routes/coins.js:185`, acotada a
`cost_points=500` para no pisar ediciones del admin). Se alineó la landing a
"1.500 puntos · aspiracional".

Era el ÚNICO lugar con 500 hardcodeado: el portal muestra los premios
dinámicos desde `GET /coins/rewards` y ya estaba bien.

## 3. 🔍 Notas para la próxima

- Los textos de puntos/monedas del portal salen de `peso_per_point` /
  `earn_per_point` en `point_config` (DB). La landing (Astro estático) los
  tiene hardcodeados — si cambia la economía, acordarse de tocar
  `HolisticPoints.astro` a mano.
- Prod API: `https://holistic-portal-api.onrender.com` (Render, auto-deploy
  a veces se cuelga — verificar que tome `b9844af`). Frontend:
  `https://portal-starter.vercel.app`.
- `server/index.js` tiene bytes no-UTF8 → usar `grep -a`.

## 4. ⏳ Pendientes (heredados, siguen vigentes)

1. **Cancelar en Admin → Pedidos** las órdenes de prueba del diagnóstico del
   14/07: `HLS-G5DF-4KP3` y `HLS-B6E7-A56E` (email
   `test-claude@lemonsapp.dev`), y la orden de prueba del cliente si sigue.
2. **PDF guía hidroponia Race (105 MB)** — sigue untracked en `docs/` local
   (límite GitHub 100 MB). Opciones: comprimir con Ghostscript, Git LFS, o
   Drive. La guía de sustrato (7 MB) ya está en el repo.
3. Verificar que Render y Vercel hayan deployado `b9844af` (el cambio de
   esta sesión es 100% frontend/landing, así que lo crítico es Vercel).
