# HANDOFF — 2026-07-18-d · Registro con captcha (sin mail) + video Day 0 para todos

> Cuarta sesión del día (continúa `HANDOFF-2026-07-18-c.md`). Dos frentes:
> el registro dejó de depender del mail de verificación (captcha matemático
> "VERIFICÁ QUE SOS HUMANO") y el video de la interna Day 0 ahora se mueve
> para el 100% de los usuarios mobile (fallback webp animado).

## 1. Commits de esta sesión

| Commit | Qué |
|--------|-----|
| `bdad09f` | **Registro: captcha matemático + verificación por email apagada**. `rules.email_verify_required` default `true → false` (catálogo, público, defaults client/wizard) y el código ahora la RESPETA (antes se ignoraba, mismo patrón que signup_mode): con la regla off el user nace `email_verified=true`, coins de bienvenida en el register (no en verify) y el login no exige mail — entran también los users viejos sin verificar. `GET /auth/captcha` → suma 2..9+2..9 + JWT con hash de la respuesta (15 min); register valida server-side (`CAPTCHA_FAILED`). Guard anti doble-crédito en verify-email |
| `e50f02c` | Registro: el client reintenta `GET /auth/captcha` cada 5s si falla (deploy a mitad de camino dejaba "Cargando…" fijo) |
| `5c1362c` | **Video Day 0 para todos**: webp ANIMADO del clip (640px/12fps/1.1MB) que reemplaza al `<video>` cuando el sistema bloquea autoplay (bajo consumo / ahorro de datos). `swapToFallback()` conserva clases/index/estilos GSAP e `images[]`; `playOrFallback()` unifica los 3 call-sites de play() (swap ante `NotAllowedError` + chequeo diferido 1.5s para bloqueos silenciosos) |

Todo deployado y verificado en prod (Render manual por el cliente + Vercel).

## 2. Estado del registro (verificado en prod)

- `email_verify_required: false` efectivo (el wizard NO tenía valor guardado
  que pisara el default — no hubo que tocar el paso 5).
- Flujo: registrarse → resolver la suma → cuenta activa al instante + 3 coins
  → login directo. Sin mail.
- Probado local end-to-end: captcha mal/faltante → 400 `CAPTCHA_FAILED`;
  captcha ok → `verified:true`, login inmediato, saldo 3.
- **Reversible**: cuando Resend esté configurado, prender el checkbox en
  `/admin/setup` paso 5 — todo el circuito de mail quedó intacto
  (verify-email, resend-verification, templates).

## 3. Estado del video Day 0 (verificado en prod)

Cascada final en `ProductHighlights` (aplica a cualquier highlight con
`mediaType: "video"` + `poster` + `fallback` en `productDetails.js`):

1. `autoplay muted playsinline` nativo → arranca solo (mayoría).
2. Sistema bloquea (`NotAllowedError` o silencio) → swap automático a
   `highlight-01-animado.webp` — las imágenes animadas no tienen política
   de autoplay, se mueven SIEMPRE.
3. Botón de play nativo de iOS oculto por CSS; gesto (touch/click) también
   reintenta play.

Si a alguien le sigue apareciendo el botón de play → tiene cacheada la
versión vieja: refrescar la página.

## 4. Aprendizajes / trampas

- **Otra regla del wizard ignorada por el código**: `email_verify_required`
  (van 4: signup_mode, coins_on_register, Resend config, esta). Quedó
  respetada. Si algo "no responde al wizard", buscar el hardcode.
- **Deploy skew otra vez**: Vercel al día + Render viejo → el captcha quedaba
  en "Cargando…" (404 del endpoint). Mitigado con retry de 5s en el client.
  Render sigue SIN auto-deploy: cada push al server necesita deploy manual.
- El autoplay de `<video>` nunca va a ser 100%: modo bajo consumo lo bloquea
  a nivel OS. El único "siempre se mueve" es imagen animada (webp/gif).
- `git add` con paths repo-relativos falla si el cwd quedó en un subdir
  (`landing/landing/...`) — correr git desde la raíz.

## 5. Pendientes

- [ ] **Resend** (igual que ayer, ahora sin urgencia — el registro ya no
      depende del mail): records DNS (MX + 2 TXT) en Cloudflare nube gris +
      API key/from en wizard. Al activarlo, evaluar re-prender
      `email_verify_required` en wizard paso 5.
- [ ] Revisar auto-deploy de Render (hoy hicieron falta 3 deploys manuales).
- [ ] OK del cliente: registro con captcha en su celu + video Day 0 en los
      teléfonos que fallaban (post refresh).
- [ ] Sin trackear: `DNSCLOUDFLARE.png` (raíz, obsoleto — borrar) y
      `docs/GUIA DE USO HIDROPONIA LÍNEA RACE.pdf` (decidir si se commitea).
- [ ] Arrastrados: borrar `portal-starter-api`, toggle Face ID OFF, sitemap,
      reward de referidos, logs 5xx.

## Referencias
- Sesiones del día: `HANDOFF-2026-07-18.md` (go-live) → `-b` (registro
  abierto + coins 3 + fix mailer) → `-c` (IG 20 pts + video mobile) → esta.
- Runbook go-live: `docs/GO-LIVE-2026-07-18.md`.
