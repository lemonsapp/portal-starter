# Handoff — 2026-07-01/02

Estado: `main` limpio, todo commiteado. Último commit `d655941`.
(Falta un `git push` final — ver más abajo.)

## Lo que se hizo esta sesión

### 1. Verificación del deploy legal (cerró lo pendiente del handoff anterior)
- Server en Render (`holistic-portal-api.onrender.com`) confirmado con el código nuevo:
  - `POST /auth/register` sin `terms_accepted` → 400 pidiendo aceptación.
  - Con `terms_accepted:true` + invite falso → pasa términos, frena en invite. Flujo intacto.
  - Columnas `terms_accepted_at`/`terms_version`/`terms_accepted_ip` auto-creadas en boot.
- Landing legal live en Vercel: `/terminos` y `/privacidad` → 200, versión 2026-06-27.
- Portal client: chunk `Register-*.js` en prod con `rg-terms` + `terms_accepted`.
- Detalle registrado en `docs/HANDOFF-2026-06-27.md` (sección "✅ VERIFICADO").
- Cabo suelto (menor): confirmar contra DB que un registro **exitoso** graba los campos (requiere invite real; no se hizo para no crear usuario basura).

### 2. Video del hero del home (`642383f`)
- Reemplazado por `BANNER-HOLISTIC-INICIO.mp4` (línea RACE, fondo blanco), provisto por el cliente.
- Se mantuvieron los nombres servidos → **no se tocó `VideoHero.astro`**. Solo swap de assets:
  - `landing/public/video/BANNER-HOME-WEB.mp4` — 1376×768, ~0.97MB (crf24, faststart)
  - `landing/public/video/BANNER-HOME-MOBILE.mp4` — 720×402, ~0.25MB (crf27, faststart)
  - `landing/public/imagenes-web/video-posters/BANNER-HOME-poster.jpg` — frame @4.5s (el clip hace fade-in desde negro; frame 0 salía negro).
- Herramienta: solo `ffmpeg` disponible (no cwebp/imagemagick).

### 3. Imagen medida 250 de la interna RACE (`d655941`)
- Reemplazado `landing/public/imagenes-web/productos/linea-race/250ml/race-1-verde-tarro.webp` por el render nuevo (`IMAGENES-HOLISTIC/renders ultimos/250-ML-RENDER.png` → webp alpha, 1086×1086, q82, 53KB).
- Mismo path → sin cambios de código. Afecta:
  - Interna RACE, medida 250 (`landing/src/data/productDetails.js:314`) — lo pedido.
  - Shop `race-1-npk-250ml` (`server/routes/shop.js:752`, `client/src/lib/shopImages.js:48`) — mismo archivo, queda consistente. La DB ya apunta al path, no hay migración.

## PENDIENTE

1. **`git push origin main`** — quedan sin pushear `642383f` (video) y `d655941` (imagen 250).
2. **Video vertical del hero para mobile** (lo que quedó charlando al final):
   - El hero es full-viewport con `object-fit: cover`. El video actual es **horizontal** (16:9); en celular vertical se recorta a una franja central y se pierden los envases de los costados.
   - **Necesitamos que el cliente exporte una versión vertical** del clip (envases reacomodados en vertical), idealmente **1080×1920 (9:16)**. Alternativas menos agresivas: 1080×1350 (4:5) o 1080×1440 (3:4).
   - Cuando llegue: encodear y conectar como `data-src-mobile` en `VideoHero.astro` (el componente ya soporta un mp4 distinto para mobile vía `data-src-mobile` — ver `landing/src/scripts/video-hero-animations.js` ~línea 61). Desktop se queda con el horizontal.

## Notas de entorno (de la sesión anterior, siguen valiendo)
- Portal real vive en `portal-starter.vercel.app`. `hgrowshop.com/portal` es el WordPress del cliente (404). `api.hgrowshop.com` aún no resuelve DNS → usar `holistic-portal-api.onrender.com`.
- DB real + Cloudinary viven SOLO en Render; el `.env` local apunta a DB stale.
- Detección de deploy Vercel: los hashes del bundle siempre difieren → grepear strings únicos en el chunk live.
- `server/index.js` es "binary" para grep (emojis) → usar `grep -a`.
- Checks de build: `node -c server/index.js` y `cd client && npm run build`.
