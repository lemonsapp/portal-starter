# HANDOFF — 2026-07-10 — MercadoPago EN PRODUCCIÓN + transferencia bancaria + fixes de landing/admin

> Continúa `HANDOFF-2026-07-09.md`. Todo commiteado + pusheado a `origin/main`
> (último: ver `git log`). Sesión larga con el hito grande del proyecto:
> **la tienda cobra de verdad**.

## 1. 💳 MercadoPago conectado EN PRODUCCIÓN (hito)

El código de Checkout Pro existía desde F2 pero nunca se había conectado
(0 filas `mercadopago.*` en `app_config`). Hoy se completó todo el circuito
con el usuario paso a paso:

### Bugs que hubieran roto el flujo el día 1 (arreglados, `e728628`)

1. **`notification_url` apuntaba al host equivocado**: se armaba con
   `APP_URL` (sitio en Vercel) pero el sitio NO tiene rewrite `/api/*` →
   Render. MP hubiera notificado a un 404 y toda orden quedaba
   `pending_payment` para siempre. Ahora usa
   `API_PUBLIC_URL || RENDER_EXTERNAL_URL` (automática en Render) con
   fallback a `APP_URL` + `console.warn`. `back_urls` siguen con `APP_URL`
   (son páginas del sitio). `API_PUBLIC_URL` documentada en
   `server/.env.example`.
2. **La guía del wizard mostraba la URL de webhook con
   `window.location.origin`** (mismo bug). Ahora muestra `${API}/api/webhooks/…`
   con el webhook_secret tipeado interpolado.

### "Probar conexión" para MP (`e728628` + `acfe690`)

Nuevo provider `mercadopago` en `POST /api/admin/config/test/:provider`:
pega a `GET /users/me`, devuelve cuenta + site_id + si es sandbox.
**Ojo formato nuevo**: MP ya no usa prefijo `TEST-` para credenciales de
prueba de apps nuevas — emite `APP_USR-…` del *usuario de prueba*; se
detecta por tag `test_user` / nickname `TESTUSER*` (`acfe690`).

### Proceso completado con el usuario

- App "Holistic Shop" (Checkout Pro) creada en la cuenta MP del cliente.
- Credenciales de prueba → compra de prueba end-to-end **verificada**:
  orden `HLS-XSRW-7GJC` pasó a `paid` SOLA vía webhook (mp_payment_id
  `167312452729`). Para pagar en sandbox hizo falta un **comprador de
  prueba** (error "una de las partes es de prueba" si pagás con cuenta real).
- Swap a **credenciales de producción** → "Probar conexión" OK.
- El webhook NO se configuró en el panel de MP: no hace falta —
  `notification_url` va por preferencia en cada checkout.
- ⚠️ Pendiente de limpieza: marcar `HLS-XSRW-7GJC` como Cancelada desde el
  admin (orden de prueba, quedó "Pagada" y ensucia stats).

## 2. 🏦 Pago por transferencia bancaria (feature nueva, `cd2964d`)

Pedido del cliente mid-sesión. Flujo completo:

1. Checkout ofrece **MercadoPago o Transferencia** (selector nuevo; la
   opción aparece sólo si `app_config → shop.transfer_details` tiene datos).
2. Transferencia → orden `pending_payment` sin preferencia MP; la success
   page muestra los **datos bancarios** (Banco Galicia / CBU / alias,
   seed one-shot `seed-transfer-details`; editable en `/setup-admin` paso
   Shop) + monto + copiar + **subir comprobante** (foto/captura →
   Cloudinary `portal-receipts`).
3. Admin ve 🏦 en Pedidos, abre el detalle con el **comprobante embebido**
   y aprueba con el botón "Pagado" de siempre (dispara email de
   confirmación). Broadcast in-app al admin cuando entra un comprobante.

Detalles técnicos:
- `orders.payment_method` (`CHECK mercadopago|transfer`) +
  `orders.transfer_receipt_url`.
- `GET /api/shop/payment-methods` (público) le dice al checkout qué ofrecer.
- Endpoint público de comprobantes con hardening post-review:
  `uploadLimiter` 20/h/IP, allow-list JPG/PNG/WebP/HEIC (sin SVG — XSS),
  guards 404/409, re-subir reemplaza y borra el asset viejo, 503 amable si
  Cloudinary no está configurado.
- Review del code-reviewer dejó 2 LOW conocidos: conexión de pool retenida
  post-COMMIT (patrón pre-existente de todo el checkout) y los datos
  bancarios del cliente viven en el seed de `migrate()` — **cuando
  portal-starter se re-use como boilerplate hay que pelar este seed junto
  con todos los seeds Holistic** (deuda ya conocida del repo).

## 3. ⚠ Incidente: Render no deployaba desde hacía ~38 h

La verificación pendiente del handoff anterior (portadas Race) falló al
inicio: prod seguía en 250ml porque **Render nunca tomó `3d36d46`** (ni
nada posterior). El usuario lo destrabó con **Manual Deploy → Deploy
latest commit** en el dashboard. Tras el deploy: las 5 familias Race
abren en `-500ml` en prod ✓ (verificado por API). Si vuelve a pasar:
mismo remedio; revisar por qué el auto-deploy quedó colgado.

## 4. Admin en mobile (`08c66cd`)

El cliente reportó "no me aparece el botón admin en mobile". Diagnóstico
con Playwright a 390px logueado como admin: **el acceso ya existía** —
en celular se llega tocando el **avatar** (arriba a la derecha) → drawer →
"Admin". Si no aparece: no está logueado o la cuenta no es admin.

Único bug real encontrado: en el **editor de producto** la barra sticky
no entraba y "Guardar cambios" quedaba cortado fuera de pantalla. Fix en
`admin/ui.jsx` (≤700px: título a fila completa + acciones en su fila;
"Cerrar" oculto en mobile porque duplica "← Volver"). El resto del panel
ya estaba sano (staff-mobile.css + tabs con scroll).

## 5. Landing: ajustes de la sesión

- **Race "Elegí el formato"** (`1f4b8c5`): tarros 250/500ml/1L de
  `imgScale` 0.85 → **0.75** (pedido "achicar un poco más los potes").
  Bidones sin tocar. Se edita en `landing/src/data/productDetails.js`.
- **Cloner hero** (`39aa171`): el banner (2560×1707) con `cover` en un
  hero de ~1780px de alto zoomeaba +85% y **los potes no se veían**.
  Scoped `[data-slug="cloner"]`: `object-fit: contain` + fondo del
  contenedor en el blanco del banner (RGB 248,246,249 sampleado) — los 4
  potes visibles, empalme invisible bajo el scrim. Verificado con
  screenshots desktop + mobile.

## Pendientes

1. **Verificar transferencia en prod** al cierre de sesión estaba
   deployando Render. Chequear:
   `curl -s https://holistic-portal-api.onrender.com/api/shop/payment-methods`
   → esperado `"transfer": true` con los datos Galicia. Después una compra
   de prueba eligiendo transferencia (se puede cancelar desde el admin).
2. 🗑 Cancelar la orden de prueba `HLS-XSRW-7GJC` desde el admin.
3. Email de "orden creada" para transferencias podría incluir los datos
   bancarios + link para subir comprobante (hoy el email es genérico).
4. Heredados: precios Elite Max 5/10L (¿par o unidad?), borrar fila
   `elite-parte-1-500g`, rotar credencial DB dev en Neon, botón masivo
   "agregar medida a toda la línea", unificar `shop_migrations` +
   `shop_migration_flags`.
5. Cuando se clone portal-starter para OTRO cliente: pelar seeds Holistic
   de `migrate()` (productos, race-portadas, transfer-details, etc.).

## Commits de la sesión (orden cronológico)

```
e728628 feat(shop): preparar la conexión de MercadoPago — webhook al host de la API + test de credenciales
acfe690 fix(shop): detectar credenciales MP de prueba con el formato nuevo APP_USR
1f4b8c5 style(landing): achicar los tarros de Race en "Elegí el formato" (0.85 → 0.75)
08c66cd fix(admin/mobile): barra del editor de producto sin cortar en celular
39aa171 fix(landing): hero de Cloner muestra los potes enteros (contain en vez de cover)
cd2964d feat(shop): pago por transferencia bancaria con comprobante y aprobación del admin
709a739 chore: sacar capturas de pantalla temporales del repo
```
